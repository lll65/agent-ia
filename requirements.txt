# Web server
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.7
aiofiles>=23.2
python-multipart>=0.0.9
python-dotenv>=1.0

# Media
moviepy==1.0.3
gTTS>=2.5
Pillow>=10.3,<11

# Search
duckduckgo-search>=6.1
requests>=2.32

# Memory vectorielle (LLM local, sans API)
chromadb>=0.5.3,<0.6

# Memory persistante (optionnel — Supabase Postgres + pgvector)
psycopg2-binary>=2.9

# Clients LLM
ollama>=0.3
groq>=0.9
openai>=1.0

# Finance & Data
yfinance>=0.2.40
pandas>=2.0
matplotlib>=3.8

# Robustesse
tenacity>=8.2

# Vidéo IA (fal.ai — crédits gratuits à l'inscription → fal.ai/dashboard/keys)
fal-client>=0.10

# UI
gradio>=4.36,<5

# Bots (optionnels — installés mais non démarrés sans token)
python-telegram-bot>=21.3
discord.py>=2.3
