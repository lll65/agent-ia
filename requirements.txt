# Web server — ensemble COHÉRENT gradio 4.44.1 (gère les onglets imbriqués, contrairement
# à 4.36 qui figeait l'UI). fastapi 0.112 + starlette 0.38 = compatibles, pas de crash template.
fastapi==0.112.2
starlette==0.38.2
uvicorn>=0.29
httpx>=0.27
pydantic>=2.7,<2.9
aiofiles>=23.2
python-multipart>=0.0.9
python-dotenv>=1.0

# Media
moviepy==1.0.3
gTTS>=2.5
Pillow>=10.3,<11

# Search
duckduckgo-search>=6.1
requests>=2.32

# Memory vectorielle
chromadb>=0.5.3,<0.6

# Memory persistante (optionnel — Supabase)
psycopg2-binary>=2.9

# Lecture de documents (PDF)
pypdf>=4.0

# Clients LLM
ollama>=0.3
groq>=0.9
openai>=1.0

# Finance & Data
yfinance>=0.2.40
pandas>=2.0
matplotlib>=3.8

# Robustesse
tenacity>=8.2

# Vidéo IA
fal-client>=0.10

# UI — gradio 4.44.1 va de pair avec gradio_client 1.3.0 (gère les onglets imbriqués)
gradio==4.44.1
gradio_client==1.3.0

# Bots
python-telegram-bot>=21.3
discord.py>=2.3
